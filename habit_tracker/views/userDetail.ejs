<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Detail</title>
  <link rel="stylesheet" href="/path/to/your/css/styles.css">
  <style>
    /* Đặt một kiểu font cho toàn bộ trang */
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f9;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 20px;
    }

    /* Định dạng tiêu đề chính */
    h1 {
      text-align: center;
      margin-bottom: 20px;
      color: #333;
    }

    /* Định dạng tiêu đề phụ */
    h2 {
      text-align: center;
      margin-bottom: 10px;
      color: #555;
    }

    /* Định dạng tiêu đề cấp ba */
    h3 {
      text-align: center;
      margin-bottom: 10px;
      color: #777;
    }

    /* Định dạng danh sách */
    ul {
      list-style-type: none;
      padding: 0;
      width: 100%;
      max-width: 600px;
    }

    /* Định dạng cho các phần tử trong danh sách */
    ul li {
      background-color: #fff;
      padding: 10px;
      margin: 5px 0;
      border-radius: 4px;
      box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
    }

    /* Định dạng đoạn văn bản thông báo không tìm thấy thói quen */
    p {
      text-align: center;
      color: #999;
    }

    /* Định dạng chung cho các phần tử trong trang */
    .container {
      background-color: #fff;
      padding: 20px 30px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      border-radius: 8px;
      width: 100%;
      max-width: 800px;
    }
  </style>
</head>

<body>
  <h1>User Detail</h1>
  <h2>
    <%= user.name_user %> (<%= user.email_user %>)
  </h2>
  <h3>Habits</h3>
  <% if (user.habits.length> 0) { %>
    <ul>
      <% user.habits.forEach(function(habit) { %>
        <li>
          <%= habit.habitName %>
        </li>
        <% }); %>
    </ul>
    <% } else { %>
      <p>No habits found.</p>
      <% } %>
</body>

</html>